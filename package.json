{
  "name": "history-map",
  "version": "1.0.0",
  "description": "World political boundaries over time — data pipeline and map front end.",
  "main": "index.js",
  "scripts": {
    "pull-source": "node scripts/pull-source.js",
    "build-data": "node scripts/build-data.js",
    "pull-cshapes": "node scripts/pull-cshapes.js",
    "build-cshapes": "node scripts/build-cshapes.js",
    "build-manifest": "node scripts/build-manifest.js",
    "build-wikidata": "node scripts/build-wikidata-matches.js",
    "pull-ohm": "node scripts/pull-ohm.js",
    "build-ohm": "node scripts/build-ohm.js",
    "build-all-data": "npm run pull-source && npm run build-data && npm run pull-cshapes && npm run build-cshapes && npm run build-manifest",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "mapshaper": "^0.7.46",
    "osmtogeojson": "^3.0.0-beta.5",
    "sharp": "^0.35.3",
    "vite": "^8.1.5"
  },
  "dependencies": {
    "leaflet": "^1.9.4",
    "topojson-client": "^3.1.0"
  }
}
